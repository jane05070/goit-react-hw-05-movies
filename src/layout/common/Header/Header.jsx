import { HeaderContainer } from './Header.styled';
import { Navigation } from '../Navigation/Navigation';

export const Header = () => {
  return (
    <HeaderContainer>
      <Navigation />
    </HeaderContainer>
  );
};
